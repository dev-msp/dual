import { createEffect, createSignal, onCleanup } from "solid-js";

export const Review = () => {
  return (
    <div class="container">
      <h1>Review</h1>
      <p>Comparison interface will go here</p>
    </div>
  );
};
